
/@media/.new/[name=blue]

./*
../

input ->path? expression [path expression]+
expression -> expression
expression -> token (. .. ^@?[a-zA-Z0-9_-#])
expression -> [attr (logic value)?]*
expression -> token?[attr logic value]*
value -> string or quoted string
path // / ./ ../ 

logic and or | = != <>

$expression = [];

$string = '';

if ($string[0] == '/') 

$string = 'getRoot()'

$string = substr($string, 1);

parse expression